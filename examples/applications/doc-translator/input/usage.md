# Usage

Install via cargo:

```bash
cargo add agentflow-core
```

Then build a flow:

```rust
let nodes = vec![/* your nodes here */];
let flow = Flow::new(nodes);
```

For more, see the [reference docs](https://docs.rs/agentflow-core).
